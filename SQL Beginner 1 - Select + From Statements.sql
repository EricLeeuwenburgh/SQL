/*
Select Statement
*, Top, Distinct, Count, As, Max, Min, Avg
*/

-- SELECT *
-- SELECT TOP 5 *
-- SELECT DISTINCT(Gender)
-- SELECT COUNT(Lastname) AS LastNameCount
-- SELECT MIN(Salary)
-- SELECT MAX(Salary)
-- SELECT AVG(Salary)

SELECT *

FROM SQL_Tutorial.dbo.EmployeeSalary	-- working in 'master' instead of directly from the SQL_Tutorial db